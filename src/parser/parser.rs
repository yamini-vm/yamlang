use crate::token::Token;
use crate::node::Node;

pub struct Parser {

}

impl Parser {
    pub fn new() -> Parser {
        Parser {

        }
    }

    pub fn parse(&self, tokens: Vec<Token>) -> Node {
        Node::new()
    }
}